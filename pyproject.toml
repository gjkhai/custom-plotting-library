[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "CustomPlottingLibrary"
version = "1.0.0"
dependencies = ["setuptools", "pandas", "numpy", "matplotlib"]
requires-python = ">=3.11"
authors = [
  {name = "gjkhai"}
]

