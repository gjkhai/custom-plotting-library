[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "cpl"
version = "1.0.0"
dependencies = ["pandas", "numpy", "matplotlib"]
requires-python = ">=3.11"
authors = [
  {name = "gjkhai"}
]



